# Configuración del servidor VPC
# Copia este archivo a .env y personaliza con tus valores
# IMPORTANTE: .env NO se sube a Git (está en .gitignore)

# ==========================================
# INFORMACIÓN DEL SERVIDOR
# ==========================================

# IP pública del servidor
SERVER_IP=1.2.3.4

# Dominio principal
DOMAIN=ejemplo.com

# Email para Let's Encrypt y notificaciones
EMAIL=admin@ejemplo.com

# ==========================================
# SSH
# ==========================================

# Puerto SSH (22 por defecto, cambiar para mayor seguridad)
SSH_PORT=22

# Usuario SSH (root o tu usuario)
SSH_USER=root

# ==========================================
# NGINX + SSL
# ==========================================

# Subdominios adicionales (separados por espacio)
SUBDOMAINS="www blog api"

# Renovación automática de SSL (días antes de expirar)
SSL_RENEW_DAYS=30

# ==========================================
# WIREGUARD VPN
# ==========================================

# Puerto UDP para WireGuard
VPN_PORT=51820

# Red interna de la VPN
VPN_NETWORK=10.8.0.0/24

# IP del servidor en la VPN
VPN_SERVER_IP=10.8.0.1

# DNS servers para clientes VPN (Cloudflare por defecto)
VPN_DNS=1.1.1.1,1.0.0.1

# ==========================================
# MONITOREO
# ==========================================

# Opción de monitoreo: basic, netdata, prometheus-grafana
MONITORING=netdata

# Dominio para Netdata
NETDATA_DOMAIN=monitor.ejemplo.com

# Usuario para acceso a Netdata
NETDATA_USER=admin

# Password para Netdata (se generará automáticamente si está vacío)
NETDATA_PASSWORD=

# Dominio para Grafana
GRAFANA_DOMAIN=grafana.ejemplo.com

# Password para Grafana admin (se generará automáticamente si está vacío)
GRAFANA_PASSWORD=

# Dominio para Prometheus (opcional)
PROMETHEUS_DOMAIN=prometheus.ejemplo.com

# ==========================================
# ALERTAS POR EMAIL
# ==========================================

# Habilitar alertas por email (true/false)
EMAIL_ALERTS=false

# Servidor SMTP
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587

# Credenciales SMTP
SMTP_USER=tu-email@gmail.com
SMTP_PASSWORD=tu-password-app

# Email destino para alertas
ALERT_EMAIL=alertas@ejemplo.com

# ==========================================
# BACKUP
# ==========================================

# Directorio local para backups
BACKUP_DIR=./backups

# Encriptar backups automáticamente (true/false)
ENCRYPT_BACKUPS=true

# Retención de backups (días)
BACKUP_RETENTION=30

# ==========================================
# DOCKER (si usas Prometheus + Grafana)
# ==========================================

# Red de Docker para monitoreo
DOCKER_NETWORK=monitoring

# Retención de datos en Prometheus (días)
PROMETHEUS_RETENTION=30

# ==========================================
# TELEGRAM (opcional - para alertas)
# ==========================================

# Bot token de Telegram
TELEGRAM_BOT_TOKEN=

# Chat ID para recibir alertas
TELEGRAM_CHAT_ID=

# ==========================================
# NOTAS
# ==========================================

# - Después de copiar este archivo a .env, edita los valores
# - .env no se subirá a Git por seguridad
# - Para Gmail, usa "App Password" en vez de tu password normal
# - Generadores de passwords: openssl rand -base64 16
